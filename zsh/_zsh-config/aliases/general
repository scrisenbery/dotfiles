# //TODO ls alias looks good, need to look at others although they look interesting
alias ls="ls --color"

# Determine if port is in use
port() {lsof -i tcp:$1}

alias serve="python -m SimpleHTTPServer" # Local static web server from current dir
